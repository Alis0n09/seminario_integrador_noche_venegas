// Con tipo explícito
const nombre: string  = "Maria Fernandez";
const sueldo: number  = 850.00;
const activo: boolean = true;

// Sin tipo — TypeScript lo infiere automáticamente del valor
const nombre2 = "Carlos Mendoza";   // TypeScript sabe que es string
const sueldo2   = 1200.50;          // TypeScript sabe que es number
const activo2 = true;               // TypeScript sabe que es boolean
